name: CI

on:
  push:
    tags-ignore:
      - v[1-9]+.[0-9]+.[0-9]+
  workflow_call:



jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Greeting
        run: echo "Hello, world!"

      - name: test 
        run: go test ./...
